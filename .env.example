# Copy this file to .env and fill in real values. Never commit .env itself.

# Postgres connection string for Prisma (see docs/ARCHITECTURE.md and
# docs/ENVIRONMENT.md). Example shape only — replace with your own
# Neon/Supabase (or other) connection string:
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DBNAME?sslmode=require"
DATABASE_URL=
